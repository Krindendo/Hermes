{
  "name": "jest-presets",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "clean:linux": "rm -rf node_modules && rm -rf .turbo",
    "clean:win": "rmdir /s /q node_modules"
  },
  "dependencies": {
    "ts-jest": "^26.5.0"
  }
}
