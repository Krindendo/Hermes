{
  "name": "jest-presets",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf node_modules && rm -rf .turbo"
  },
  "dependencies": {
    "ts-jest": "^26.5.0"
  }
}
